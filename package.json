{
  "name": "react-extension-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "esbuild app.jsx --bundle --outfile=dist/app.js",
    "start": "node server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.12.19",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
